<Window x:Class="ElevenLabsStudio.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ElevenLabs Studio"
        Height="820" Width="1320"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        FontFamily="{DynamicResource MaterialDesign.Font}">
    <materialDesign:DialogHost>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 1. TopAppBar -->
            <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16,10">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Button x:Name="OpenSettings"
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="设置"
                                Margin="0,0,4,0">
                            <materialDesign:PackIcon Kind="Cog" />
                        </Button>
                        <Button x:Name="OpenHelp"
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="帮助">
                            <materialDesign:PackIcon Kind="HelpCircleOutline" />
                        </Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="MicrophoneMessage"
                                                 Width="22" Height="22"
                                                 Margin="0,0,8,0"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="ElevenLabs Studio"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </DockPanel>
            </materialDesign:ColorZone>

            <!-- 2. Main split: left agent menu / right detail -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ContentControl Grid.Column="0" x:Name="AgentsVm" />

                <materialDesign:Card Grid.Column="1" Padding="16">
                    <ContentControl x:Name="AgentDetail" />
                </materialDesign:Card>
            </Grid>

            <!-- 3. StatusBar -->
            <materialDesign:ColorZone Grid.Row="2" Mode="PrimaryLight" Padding="16,4">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding CurrentTime, StringFormat='{}{0:HH:mm:ss}'}"
                               VerticalAlignment="Center"
                               FontSize="12" />
                    <StackPanel Orientation="Horizontal">
                            <ProgressBar Style="{DynamicResource MaterialDesign.LinearProgressBar}"
                                         Width="120"
                                         IsIndeterminate="True"
                                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                         Margin="0,0,12,0" />
                            <TextBlock Text="{Binding BusyMessage}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                </DockPanel>
            </materialDesign:ColorZone>
        </Grid>
    </materialDesign:DialogHost>
</Window>