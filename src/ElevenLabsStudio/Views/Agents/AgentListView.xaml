<UserControl x:Class="ElevenLabsStudio.Views.Agents.AgentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:domain="clr-namespace:ElevenLabsStudio.Core.Domain;assembly=ElevenLabsStudio.Core">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="1.4*" />
        </Grid.ColumnDefinitions>

        <!-- Left: agent list -->
        <DockPanel Grid.Column="0" Margin="0,0,12,0">
            <materialDesign:ColorZone Mode="PrimaryMid" Padding="8" DockPanel.Dock="Top">
                <DockPanel>
                    <Button x:Name="LoadAsync"
                            DockPanel.Dock="Right"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Refresh">
                        <materialDesign:PackIcon Kind="Refresh" />
                    </Button>
                    <TextBlock Text="Agents"
                               VerticalAlignment="Center"
                               FontSize="16"
                               FontWeight="SemiBold" />
                </DockPanel>
            </materialDesign:ColorZone>
            <ListBox x:Name="Agents"
                     SelectedItem="{Binding SelectedAgent, Mode=TwoWay}"
                     DisplayMemberPath="Name"
                     HorizontalContentAlignment="Stretch" />
        </DockPanel>

        <!-- Right: selected agent detail + edit panel -->
        <ContentControl Grid.Column="1"
                        x:Name="UpdateAgent"
                        cal:View.Model="{Binding UpdateAgentVm}" />
    </Grid>
</UserControl>