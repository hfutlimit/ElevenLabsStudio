<UserControl x:Class="ElevenLabsStudio.Views.AgentDetail.SystemPromptTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="0,8,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 x:Name="Prompt"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Cascadia Mono, monospace"
                 FontSize="13"
                 materialDesign:HintAssist.Hint="System prompt"
                 Padding="10" />

        <DockPanel Grid.Row="0" Margin="0,0,0,0" LastChildFill="True"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Border DockPanel.Dock="Right"
                    Background="{StaticResource App.Surface}"
                    Padding="8,2"
                    CornerRadius="3">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="TextLength"
                                             Width="12" Height="12"
                                             Margin="0,0,4,0"
                                             VerticalAlignment="Center"
                                             Foreground="{StaticResource App.TextMuted}" />
                    <TextBlock x:Name="PromptLength"
                               Text="0 chars"
                               FontSize="11"
                               Foreground="{StaticResource App.TextSecondary}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Margin="0,12,0,0"
                   FontWeight="SemiBold"
                   FontSize="12"
                   Foreground="{StaticResource App.Text}">
            <Run Text="Local Suggestions" />
            <Run Text="(" /><Run Text="{Binding Suggestions.Count, Mode=OneWay}" /><Run Text=")" />
        </TextBlock>

        <ListBox Grid.Row="2"
                 x:Name="Suggestions"
                 Margin="0,4,0,0"
                 Background="Transparent"
                 BorderThickness="0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,2" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource App.SoftCard}" Padding="10,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Grid.Column="0"
                                                     Margin="0,0,8,0"
                                                     VerticalAlignment="Top"
                                                     Foreground="{StaticResource App.TextSecondary}">
                                <materialDesign:PackIcon.Style>
                                    <Style TargetType="materialDesign:PackIcon">
                                        <Setter Property="Kind" Value="Information" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Severity}" Value="Suggestion">
                                                <Setter Property="Kind" Value="LightbulbOutline" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                                <Setter Property="Kind" Value="AlertOutline" />
                                                <Setter Property="Foreground" Value="{StaticResource App.Danger}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </materialDesign:PackIcon.Style>
                            </materialDesign:PackIcon>
                            <StackPanel Grid.Column="1">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding FieldName}"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               Foreground="{StaticResource App.Text}" />
                                    <Border Style="{StaticResource App.Badge}" Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Severity}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource App.Accent}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding Rationale}"
                                           TextWrapping="Wrap"
                                           FontSize="11"
                                           Foreground="{StaticResource App.TextSecondary}"
                                           Margin="0,2,0,0" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>