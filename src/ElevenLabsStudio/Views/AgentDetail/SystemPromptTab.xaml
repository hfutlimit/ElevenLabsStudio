<UserControl x:Class="ElevenLabsStudio.Views.AgentDetail.SystemPromptTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="0,12,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 x:Name="Prompt"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Cascadia Mono, monospace"
                 FontSize="13"
                 materialDesign:HintAssist.Hint="System prompt" />

        <TextBlock Grid.Row="1"
                   Margin="0,12,0,4"
                   FontWeight="SemiBold">
            <Run Text="Local Suggestions" />
            <Run Text="(" /><Run Text="{Binding Suggestions.Count, Mode=OneWay}" /><Run Text=")" />
        </TextBlock>

        <ListBox Grid.Row="2" x:Name="Suggestions">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0"
                                                 Margin="0,0,8,0"
                                                 VerticalAlignment="Center">
                            <materialDesign:PackIcon.Style>
                                <Style TargetType="materialDesign:PackIcon">
                                    <Setter Property="Kind" Value="Information" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Severity}" Value="Suggestion">
                                            <Setter Property="Kind" Value="LightbulbOutline" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                            <Setter Property="Kind" Value="AlertOutline" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:PackIcon.Style>
                        </materialDesign:PackIcon>
                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding FieldName}" FontWeight="SemiBold" />
                                <Border Margin="8,0,0,0" Padding="6,1" CornerRadius="3" Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
                                    <TextBlock Text="{Binding Severity}" FontSize="10" Opacity="0.7" />
                                </Border>
                            </StackPanel>
                            <TextBlock Text="{Binding Rationale}" TextWrapping="Wrap" Opacity="0.85" Margin="0,2,0,0" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>