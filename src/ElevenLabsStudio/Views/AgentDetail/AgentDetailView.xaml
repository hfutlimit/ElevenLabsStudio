<UserControl x:Class="ElevenLabsStudio.Views.AgentDetail.AgentDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal="http://www.caliburnproject.org">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="{Binding Agent.Name}"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <materialDesign:PackIcon Kind="Identifier" Margin="0,0,4,0" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Agent.AgentId}" FontSize="12" Opacity="0.7" />
            </StackPanel>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button x:Name="Push"
                    Content="📤 Push"
                    Margin="0,0,8,0"
                    Style="{StaticResource MaterialDesignRaisedAccentButton}" />
            <Button x:Name="ReloadAsync"
                    Content="🔄 Refresh"
                    Margin="0,0,8,0" />
            <Button x:Name="DryRun"
                    Content="🧪 Dry-run"
                    Style="{StaticResource MaterialDesignFlatButton}" />
            <materialDesign:PackIcon Kind="ClockOutline"
                                     Margin="16,0,4,0"
                                     VerticalAlignment="Center" />
            <TextBlock Text="{Binding Agent.UpdatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                       VerticalAlignment="Center"
                       FontSize="12"
                       Opacity="0.7" />
        </StackPanel>

        <!-- Tabs -->
        <TabControl Grid.Row="2"
                    SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}"
                    Style="{StaticResource MaterialDesignFilledTabControl}">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="TextBox" Margin="0,0,6,0" />
                        <TextBlock Text="System Prompt" />
                    </StackPanel>
                </TabItem.Header>
                <ContentControl x:Name="SystemPromptVm" />
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CommentTextOutline" Margin="0,0,6,0" />
                        <TextBlock Text="First Msg" />
                    </StackPanel>
                </TabItem.Header>
                <ContentControl x:Name="FirstMessageVm" />
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="GraphOutline" Margin="0,0,6,0" />
                        <TextBlock Text="Workflow" />
                    </StackPanel>
                </TabItem.Header>
                <ContentControl x:Name="WorkflowVm" />
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ForumOutline" Margin="0,0,6,0" />
                        <TextBlock Text="Conversations" />
                    </StackPanel>
                </TabItem.Header>
                <ContentControl x:Name="ConversationsVm" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>