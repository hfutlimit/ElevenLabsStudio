<UserControl x:Class="ElevenLabsStudio.Views.AgentDetail.FirstMessageTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="0,8,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <TextBox x:Name="FirstMessage"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="First message — Agent 开场白"
                     FontSize="13" />
            <Border HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,8,12,0"
                    Background="{StaticResource App.Surface}"
                    Padding="8,2"
                    CornerRadius="3">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FormatLetterCase"
                                             Width="12" Height="12"
                                             Margin="0,0,4,0"
                                             VerticalAlignment="Center"
                                             Foreground="{StaticResource App.TextMuted}" />
                    <TextBlock x:Name="FirstMessageLength"
                               Text="0 chars"
                               FontSize="11"
                               Foreground="{StaticResource App.TextSecondary}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource App.SoftCard}" Padding="14">
            <StackPanel>
                <TextBlock Text="预览"
                           FontWeight="SemiBold"
                           FontSize="11"
                           Foreground="{StaticResource App.TextSecondary}"
                           Margin="0,0,0,6" />
                <TextBlock Text="{Binding FirstMessage, Mode=OneWay}"
                           TextWrapping="Wrap"
                           FontSize="14"
                           Foreground="{StaticResource App.Text}" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2"
                   Margin="0,14,0,4"
                   FontWeight="SemiBold"
                   FontSize="12"
                   Foreground="{StaticResource App.Text}">
            <Run Text="本地建议" />
            <Run Text="(" /><Run Text="{Binding Suggestions.Count, Mode=OneWay}" /><Run Text=")" />
        </TextBlock>
    </Grid>
</UserControl>