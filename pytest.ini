[pytest]
testpaths = tests/ElevenLabsStudio.E2ETests
markers =
    e2e: end-to-end tests that launch ElevenLabsStudio.exe
    unit: fast unit-style tests (kept for symmetry with C# suite)
    integration: integration tests (kept for symmetry with C# suite)
addopts = -ra -q